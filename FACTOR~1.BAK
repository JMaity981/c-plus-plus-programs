#include<iostream.h>
#include<conio.h>
long int fact(int);
int main()
{ 
  int n;
  clrscr();
  cout<<"Enter the number: "<<endl;
  cin>>n;
  cout<<"Factorial of this number is:"<<fact(n);
  getch();
  return(0);
}	
long int fact(int x)
{
  if(x==0||x==1)
    return(1);
  else
    return(x*fact(x-1));
}
